<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:bind="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.mino.contactss.ui.main.viewmodel.ContactsViewModel" />

        <variable
            name="clickHandler"
            type="com.mino.contactss.ui.common.binding.OnItemClickedListener" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            bind:onItemClickListener="@{(position) -> clickHandler.itemClicked(position)}"
            bind:swipeEnabled="@{true}" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
